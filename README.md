# topt - Token Optimization

topt is a Python library for token optimization.

## Installation

Install the required dependencies:

```bash
pip install -r requirements.txt
```

## Testing

```bash
python3 -m unittest discover
```

## Compare

```bash
python3 -m compare
```
